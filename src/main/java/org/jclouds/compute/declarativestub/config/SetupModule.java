package org.jclouds.compute.declarativestub.config;

import com.google.inject.AbstractModule;

public class SetupModule extends AbstractModule {

	@Override
	protected void configure() {
		throw new RuntimeException("Extend me !!");

	}
}
